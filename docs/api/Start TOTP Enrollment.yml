info:
  name: Start TOTP Enrollment
  type: http
  seq: 4

http:
  method: POST
  url: "{{BASE_URL}}/mfa/totp/enrollment"
  headers:
    - name: Authorization
      value: Bearer {{AccessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
