info:
  name: Get Current Account
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/accounts/@me"
  headers:
    - name: Authorization
      value: Bearer {{AccessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
